
/**
 * Returns "Hello " plus the str parameter
 * 
 * @example
 * // returns "Hello World"
 * hello("World")
 * 
 * @param {string} str the string to say hello to
 */
function hello(str) {
    return `Hello ${str}` 
}