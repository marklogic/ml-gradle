buildscript {
  repositories {
	  mavenLocal()
    jcenter()
  }
  dependencies {
    classpath "com.marklogic:ml-gradle:${mlGradleVersion}"
  }
}

apply plugin: "java"
apply plugin: "com.marklogic.ml-gradle"

repositories {
	mavenLocal()
	jcenter()
}
